name: Build coordinator image

on:
  push:
    branches:
      - main
    paths:
      - "services/coordinator/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: services/coordinator/go.sum

      - name: Run tests
        working-directory: services/coordinator
        run: go test ./...

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: services/coordinator/
          file: services/coordinator/Dockerfile
          push: true
          tags: |
            ghcr.io/brian-l-johnson/re-coordinator:${{ github.sha }}
            ghcr.io/brian-l-johnson/re-coordinator:latest
